import { StyleSheet } from "react-native";

export const cs = StyleSheet.create({
    textStyle: {
        fontSize: 24,
        fontWeight: 'bold',
        color: '#000'
    },
    colorContainer: {
        backgroundColor: '#fff',
        margin: 20,
        padding: 8,
        borderRadius: 8,
        opacity: 0.7
    },
    textBackgroundColor: {
        fontSize: 16,
        color: '#333',
    },
    colorStyle: {
        fontSize: 16,
        fontWeight: 'bold',
        color: '#333',
    }
})